import React, { FC } from "react"

/**
 * 语言切换组件
 */
const LangSwitcher: FC = () => {
    // 获取是否为 i18n 模式
    // 语言 label
    // 语言 tag
    // 并且更新 HTML 的 lang tag
    return <ul>
        <li>简体中文</li>
    </ul>
}

export default LangSwitcher
